📚 Library Hub - Prototip Uygulama

 -Bu proje, bir kütüphane yönetim sistemi prototipi olarak **Spring Boot** ve **React** kullanılarak geliştirilmiştir.

 
  Öne Çıkan Özellikler

* Google Books Entegrasyonu**: Kitap ismine göre otomatik veri çekme ve veritabanına kaydetme.
* Gelişmiş Filtreleme**: 'A' ile başlayan kitaplar ve 2023 sonrası yayınlar için özel API uçları.
* Modern UI**: Tailwind CSS ile hazırlanmış, sayfalamalı (pagination) ve custom modal içeren tablo tasarımı.

  Kurulum ve Çalıştırma

* Backend**: `mvn spring-boot:run` (PostgreSQL `library_db` veritabanı gerektirir).
* Frontend**: `npm install` && `npm run dev`
